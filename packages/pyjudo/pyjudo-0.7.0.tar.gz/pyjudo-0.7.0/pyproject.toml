[project]
name = "pyjudo"
version = "0.7.0"
description = "Simple dependency injection for Python."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "coverage>=7.6.4",
    "jupyter>=1.1.1",
    "pytest-cov>=6.0.0",
]
