[project]
name = "andres-server"
version = "0.1.1"
description = "Andre's Rainbow Land with cute kittens and AI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
 "mcp>=1.0.0",
 "starlette>=0.41.3",
]
[[project.authors]]
name = "Andre Ratzenberger"
email = "andre.ratzenberger@whiteduck.de"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
andres-server = "andres_server:main"
