[project]
name = "char_count_package"
version = "0.0.2"
authors = [
  { name="Suelen Karbivnychyy", email="author@example.com" },
]
description = "A package to count the ocurrency of unique characters in a string or file."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/pypa/sampleproject"
Issues = "https://github.com/pypa/sampleproject/issues"