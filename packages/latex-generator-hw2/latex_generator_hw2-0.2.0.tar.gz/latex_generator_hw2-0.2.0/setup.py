from setuptools import setup, find_packages

setup(
    name='latex_generator_hw2',
    version='0.2.0',
    package_dir={"": "src"},
    packages=find_packages("src"),
    install_requires=[],
    entry_points={},
)
