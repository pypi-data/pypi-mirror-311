[build-system]
requires = ["setuptools>=64","wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rsb-git"
description = "High level interface for git functionality"
license = {file = "LICENSE"}
requires-python = ">=3.8"
dependencies = []
version = "1.13.1"

[tool.setuptools.packages.find]
where = ["src"]
