[project]
name = "dsmq"
version = "0.7.0"
description = "A dead simple message queue"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
dsmq = { workspace = true }
