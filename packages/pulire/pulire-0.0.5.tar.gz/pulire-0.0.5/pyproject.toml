[tool.poetry]
name = "pulire"
version = "0.0.5"
description = "A lightweight DataFrame validation library."
authors = ["Meteostat <info@meteostat.net>", "clampr <christian.lamprecht@aol.de>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.2.3"
numpy = "^2.1.3"


[tool.poetry.group.dev.dependencies]
black = "^24.10.0"
pytest = "^8.3.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
