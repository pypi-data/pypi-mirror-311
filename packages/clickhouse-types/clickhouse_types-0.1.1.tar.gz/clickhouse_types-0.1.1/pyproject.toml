[tool.poetry]
name = "clickhouse-types"
version = "0.1.1"
description = "Converting ClickHouse types into other schemas' types"
authors = ["Cheng Chang <cheng.chang3459@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
