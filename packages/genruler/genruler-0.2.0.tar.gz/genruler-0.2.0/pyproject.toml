[tool.poetry]
name = "genruler"
version = "0.2.0"
description = ""
authors = ["Jeffrey04 <mycyberpet@yahoo.com>"]
readme = "README.md"
packages = [{include = "genruler", from = "src"}]

[tool.poetry.dependencies]
python = "^3.12"
funcparserlib = "^1.0.1"


[tool.poetry.group.dev.dependencies]
ipdb = "^0.13.13"
pytest = "^8.3.3"
pytest-cov = "^6.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
