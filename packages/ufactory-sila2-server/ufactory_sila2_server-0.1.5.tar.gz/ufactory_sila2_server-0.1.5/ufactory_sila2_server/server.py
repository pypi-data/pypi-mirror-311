# Generated by sila2.code_generator; sila2.__version__: 0.12.2

from typing import Optional
from uuid import UUID, uuid4

from sila2.server import SilaServer

from .feature_implementations.ufcontroller_impl import UFControllerImpl
from .generated.ufcontroller import UFControllerFeature


class Server(SilaServer):
    def __init__(
        self,
        server_uuid: Optional[UUID] = None,
        name: Optional[str] = None,
        description: Optional[str] = None,
    ):
        # TODO: fill in your server information
        if name is None:
            name = "UFactory Lite 6"
        if description is None:
            description = "Robotic arm"
        super().__init__(
            server_name=name,
            server_description=description,
            server_type="Arm",
            server_version="0.1",
            server_vendor_url="https://gitlab.com/SiLA2/sila_python",
            server_uuid=server_uuid if server_uuid is not None else uuid4(),
        )

        self.ufcontroller = UFControllerImpl(self)
        self.set_feature_implementation(UFControllerFeature, self.ufcontroller)
