[tool.poetry]
name = "textware"
version = "0.0.12"
description = "All tools about text"
authors = ["Kun Lu <hi@shugic.com>"]
readme = "README.md"
license = ""
packages = [{ include = "textware" }]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = '*'
numpy = "*"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
