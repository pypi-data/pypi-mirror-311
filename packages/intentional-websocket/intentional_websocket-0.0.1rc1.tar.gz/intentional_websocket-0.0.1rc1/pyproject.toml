[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "intentional-websocket"
dynamic = ["version"]
description = "Plugin that makes Intentional bots able to work with models that accept a continuous stream of input data through a WebSocket connection, such as OpenAI's Realtime API."
readme = "README.md"
requires-python = ">=3.9"
license = "AGPL-3.0-or-later"
license-files = { paths = ["LICENSE.txt"] }
keywords = []
authors = [
  { name = "ZanSara", email = "github@zansara.dev" },
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
]

dependencies = [
  "intentional-core"
]

[project.optional-dependencies]
test = [
  "intentional[test]"
]
lint = [
  "intentional[lint]"
]
dev = [
  "intentional[test,lint]"
]


[project.urls]
Documentation = "https://github.com/intentional-ai/intentional#readme"
Issues = "https://github.com/intentional-ai/intentional/issues"
Source = "https://github.com/intentional-ai/intentional"

[tool.hatch.version]
path = "src/intentional_websocket/__about__.py"

[tool.black]
line-length = 120
fast = true

[tool.pylint]
disable=[
  "fixme"
]

[tool.pylint.format]
max-line-length = 120

[tool.pylint.main]
ignore = ["tests"]
