# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from typing import List
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .pagination import Pagination
from .video import Video


@JsonMap({})
class VideoList(BaseModel):
    """VideoList

    :param pagination: Pagination response schema Used to serialize pagination metadata. Its main purpose is to document the pagination metadata.
    :type pagination: Pagination
    :param rows: rows
    :type rows: List[Video]
    """

    def __init__(self, pagination: Pagination, rows: List[Video]):
        """VideoList

        :param pagination: Pagination response schema Used to serialize pagination metadata. Its main purpose is to document the pagination metadata.
        :type pagination: Pagination
        :param rows: rows
        :type rows: List[Video]
        """
        self.pagination = self._define_object(pagination, Pagination)
        self.rows = self._define_list(rows, Video)
