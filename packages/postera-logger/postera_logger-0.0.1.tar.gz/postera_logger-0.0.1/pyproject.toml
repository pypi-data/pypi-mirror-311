[tool.poetry]
name = "postera-logger"
version = "0.0.1"
description = ""
readme = "README.md"
authors = ["PostEra engineering <engineering@postera.ai>"]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

